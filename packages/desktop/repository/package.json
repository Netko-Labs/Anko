{
  "name": "@anko/desktop-repository",
  "private": true,
  "version": "0.10.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsgo -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@anko/desktop-domain": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@anko/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  }
}
